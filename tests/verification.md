# 2026-09-16 架构修复与场景回归

- 自动回归 52 项：三方合并、同字段选择、已删除记录保护、删除关联范围、级联操作顺序、日期/依赖整体校验、独立方案连续应用、当前风险复核、原子回执与重试。旧版无基线方案仍保守拒绝覆盖。
- 真实本地 API：无关项目更新后旧编辑保存成功；同字段返回结构化冲突；确认后再次更新会重新要求核对风险；不存在的方案不能提交；新增子事项后旧删除请求被拒绝。
- 浏览器操作：底部取消出现草稿保护提示；关闭重开和刷新重开均恢复完整输入；跨项目更新后直接保存；同字段冲突保留文本，选择后保存成功。
- 弱网故障注入：6 秒延迟下，保存期间所有记录字段、取消和提交按钮锁定；请求完成后关闭表单。离线显示“当前离线 · 数据待同步”，恢复网络后重新同步。
- 真实 Qwen：三次方案生成成功，耗时约 4.0 / 3.2 / 4.4 秒；两份独立方案在浏览器连续应用成功。第三份重叠方案显示程序计算的冲突，删除一个旧安排后风险列表同步减少，应用时重新确认，返回检查未写入。
- 日程：前置推迟到 9 月 28 日后，9 月 24 日的关联日程显示等待及日期风险；完成事项的未来安排显示释放入口，点击后确认删除该预留时间。项目全景可见跨项目的前置连接。
- 检索与看板：按联系人职责“视觉确认负责人”、工程用途“上线文档”均命中；等待列新建事项默认等待状态。
- 语音生命周期：浏览器用合成振荡音模拟麦克风，阻断 ASR 请求；录音时收起须确认结束，识别失败后反复收起/打开仍保留重试入口。测试后恢复 getUserMedia 和网络设置。此项只验证恢复流程，不代表真实人声识别质量。
- 真实供应商语音回归：之前保存的合成中文 WAV 经真实 ASR 正确识别“三点，不对，改成四点到四点半”；真实 TTS 返回 226,604 字节 WAV，均 HTTP 200。未使用用户本人麦克风录制，也未宣称人工试听。
- 390×844 手机布局：输入、发送和关闭入口可见，页面宽度保持 390，无横向页面溢出。测试使用独立本地数据，结束后核对原项目、事项、日程、联系人、工程五类数据完全保留。
- 新迁移仅新增可空的 `base_records` 字段，兼容既有回执；不上传本地测试数据。TypeScript 检查与生产构建在发布前执行。

限制：网络或 Qwen 超时仍可能发生；本次修复等待反馈、输入/录音保留及幂等重试，无法保证外部服务永不超时。草稿属于当前浏览器标签页，关闭整个标签页后不承诺保留；录音刷新前应先重试。

---

# 2026-09-16 使用体验与可靠性打磨

- 33 项自动回归测试通过：含方案持久恢复、分页、放弃/应用竞争、业务版本保护、事务提交、原方案幂等、最终日程冲突、年度时间轴和长语音完整分段。
- TypeScript 检查通过；最终生产构建在本次发布前执行。
- 真实 Qwen：普通中文创建单日程方案成功；闲聊与信息不足的会议请求返回答复/澄清，没有伪造修改；故意重叠的两条日程形成可检查方案。
- 本地浏览器：关闭助手、刷新、继续聊天后恢复待确认方案；点击应用后在 9 月 18 日正确显示 15:00–15:30 的测试日程；应用后方案不再出现。测试日程已清理。
- 最终页面内确认：点击重叠方案，显示“冲突验证甲 ↔ 冲突验证乙”；返回检查关闭提示，没有应用日程。用页面内确认替代原生阻塞弹窗。
- 真实 ASR：已有合成测试音频成功识别“明天上午10点到11点安排时间管理大师的语音测试”。没有使用用户本人麦克风录音。
- 长回复 667 字分为 531 / 136 两段，均真实返回 WAV（HTTP 200）；浏览器单段朗读进入播放状态，“停止朗读”可中断。没有宣称完整长音频已逐字人工试听。
- 桌面 1512×1000、手机 390×844 检查了项目全程导航、助手、方案与日历布局；缩小引导区并保留输入区域。
- 数据迁移仅新增 proposal_state 与查询索引，保留既有工作空间和历史回执。全部测试只操作本机数据，不上传测试项目/日程。

Qwen 工具选择依据：[官方 Function Calling 文档](https://docs.modelstudio.console.alibabacloud.com/en/model-studio/qwen-function-calling)。当前非思考模式使用 required，由答复和方案两个工具明确分流；校验失败纠正一次。

---

# 使用验证记录

验证时间：2026-09-14。浏览器为本机 Edge，无真实用户信息参与测试。

## 2026-09-15 连接恢复与并发修复

线上日志确认 `/api/qwen/chat` 曾返回 409，原入口因客户端版本较旧直接拒绝聊天。浏览器的 `Failed to fetch` 未有对应服务端错误记录，无法仅由截图判定底层断网原因；本次修复请求中断后的恢复，不假定网络本身已被修复。

- 自动测试 22/22 通过：12 项领域规则，加 10 项连接恢复、SQLite 事务及回执规则。
- SQLite 实际执行生成的迁移及提交 SQL：事务回滚、同请求赢家回执、业务并发、旧方案失效、跨用户隔离、消息裁剪后回执恢复均通过。
- 客户端故障注入：模拟已经提交但响应丢失，同一请求重试取回原回复；401/409/HTML 登录页不盲目重试；取消请求不重试；网络错误转换为中文提示。
- 本机真实 Qwen + D1：旧页面 revision=0 聊天 HTTP 200；同编号重试 HTTP 200（43ms），回复一致且只保留一对消息。
- 本机真实 API：聊天后用打开编辑时的业务版本保存 HTTP 200；真正的业务变化后旧编辑请求仍返回 409，未覆盖数据。
- 本机真实 Qwen：生成方案期间更新项目，回复保存成功；方案保留原业务版本 4，最新业务版本 5，明确标记过期；同编号重试 31ms 取回原方案。
- 未应用任何测试日程，临时项目已清理。以上为本机应用与实际供应商联测，未声称模拟了用户当时的实际网络条件。

| 范围 | 结果 |
| --- | --- |
| 领域规则自动测试 | 12/12 通过；包括并行、同名、部分更新、循环依赖、删除关联、日期、幂等与冲突 |
| TypeScript 静态检查 | 通过 |
| 生产构建 | 通过 |
| 本地 D1：真实 API | 新增 200；相同操作重试不增加版本；同版本并发返回 200/409；无效批次 400 且数据不变 |
| 请求边界 | 非同源请求 403；无效音频 400；缺少通用密钥时明确 503 |
| 浏览器操作 | 项目/事项创建、悬停详情、刷新保留、日程添加与完成、看板拖动、联系人、工程路径均通过 |
| 表单失败恢复 | 起止日期无效时保留输入，显示原因 |
| 手机布局 | 390×844 检查，文档宽度 390；时间轴在自身容器内滚动 |
| 桌面布局 | 1512×1000 检查，共享时间轴、条内简称、并行分层与依赖显示正常 |
| 模拟麦克风 | 录音转 16kHz WAV 后进入识别请求；模拟返回可编辑文字；停止/取消后所有媒体轨道 ended |
| 模拟 Agent 回复 | 展示待应用方案，用户应用后真实写入本地 D1 日程 |
| WebMCP | 浏览器原生上下文不可用；用注册器测试替身验证两个工具及无效输入处理，不等同原生兼容性验证 |

## 已完成的本机真实 Qwen 联测

本次由用户在 Codex 内手动发起交互测试，使用现有 Token Plan 密钥及官方套餐专用域名。此前将套餐使用限制扩大为“不能做任何测试”的判断已纠正。本机采用 token-plan-local 模式完成下表测试。后续用户要求在 Site 测试，接入配置增加 token-plan 模式，保留本机别名；托管密钥通过 Sites secret 单独配置。

| 真实调用 | 证据 |
| --- | --- |
| qwen3.8-flash 文本连接 | HTTP 200，回复“连接成功”，用量 46 tokens |
| qwen-audio-3.0-tts-plus 合成 | HTTP 200，生成 4.72 秒、24kHz 单声道 WAV；音色 longanhuan_v3.6 |
| qwen-audio-3.0-asr-flash 识别 | HTTP 200，正确识别“明天上午10点到11点，安排时间管理大师的语音测试。” |
| 浏览器录音转换与真实 ASR | 使用上述语音作为模拟麦克风输入，通过网页录音、WAV 转换和实际供应商请求，识别正确 |
| 真实 Agent 方案 | qwen3.8-flash 生成明天 10:00–11:00 的独立测试日程，通过校验并显示方案 |
| 应用方案 | 用户交互点击应用后实际写入本地 D1；日程视图正确显示 |
| 真实 Qwen 朗读 | /api/qwen/tts 返回 200，HTMLAudioElement.play 成功，出现停止朗读按钮，停止成功 |

联测发现并修复：Cloudflare fetch 不支持 redirect:error，改为 manual 并检查 HTTP 状态；Token Plan 的 TTS 使用独立 SpeechSynthesizer 路径，ASR 使用 DashScope 多模态路径并读取 output.text。已有通用接口保留。

限制：测试音频来自 Qwen 合成，不是用户本人麦克风录制；没有测试实时 WebSocket 连续对话。网页当前采用录音→识别→编辑发送→方案应用→朗读的分段交互。单次测试的延迟和音质不代表长期性能。

官方依据：
- https://help.aliyun.com/zh/model-studio/token-plan-personal-overview
- https://help.aliyun.com/zh/model-studio/token-plan-multimodal-gen
- https://help.aliyun.com/zh/model-studio/non-realtime-speech-recognition-user-guide
